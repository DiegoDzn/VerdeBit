import { SectionHeader } from '../../../components/ui/SectionHeader';
import { Screen } from '../../../components/ui/Screen';

export function CatalogScreen() {
  return (
    <Screen>
      <SectionHeader
        title="Catálogo"
        description="Flora y fauna del humedal Vegas de Chivilcán."
      />
    </Screen>
  );
}
