import { SectionHeader } from '../../../components/ui/SectionHeader';
import { Screen } from '../../../components/ui/Screen';

export function CalendarScreen() {
  return (
    <Screen>
      <SectionHeader
        title="Calendario"
        description="Actividades comunitarias, talleres y recorridos."
      />
    </Screen>
  );
}
