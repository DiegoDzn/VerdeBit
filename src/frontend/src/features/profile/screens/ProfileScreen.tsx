import { SectionHeader } from '../../../components/ui/SectionHeader';
import { Screen } from '../../../components/ui/Screen';

export function ProfileScreen() {
  return (
    <Screen>
      <SectionHeader
        title="Perfil"
        description="Progreso del estudiante, puntos y medallas."
      />
    </Screen>
  );
}
