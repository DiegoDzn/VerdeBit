import { SectionHeader } from '../../../components/ui/SectionHeader';
import { Screen } from '../../../components/ui/Screen';

export function QuizScreen() {
  return (
    <Screen>
      <SectionHeader
        title="Quiz"
        description="Desafíos interactivos con puntos y medallas."
      />
    </Screen>
  );
}
